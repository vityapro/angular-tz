import { EndingProductPipe } from './ending-product.pipe';

describe('EndingProductPipe', () => {
  it('create an instance', () => {
    const pipe = new EndingProductPipe();
    expect(pipe).toBeTruthy();
  });
});
