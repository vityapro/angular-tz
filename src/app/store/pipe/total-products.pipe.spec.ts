import { TotalProductsPipe } from './total-products.pipe';

describe('TotalProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
